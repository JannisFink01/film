import dotenv from 'dotenv';
import process from 'node:process';
dotenv.config();
const { NODE_ENV, PORT, FILM_SERVICE_HOST, FILM_SERVICE_PORT, K8S_TLS, DB_TYPE, DB_NAME, DB_HOST, DB_USERNAME, DB_PASSWORD, DB_PASSWORD_ADMIN, DB_POPULATE, APOLLO_DEBUG, LOG_LEVEL, LOG_DIR, LOG_PRETTY, LOG_DEFAULT, HEALTH_PRETTY_PRINT, JWT_EXPIRES_IN, JWT_ISSUER, SMTP_DEACTIVATED, SMTP_HOST, SMTP_PORT, SMTP_LOG, START_DB_SERVER, USER_PASSWORD_ENCODED, } = process.env;
export const env = {
    NODE_ENV,
    PORT,
    FILM_SERVICE_HOST,
    FILM_SERVICE_PORT,
    K8S_TLS,
    DB_TYPE,
    DB_NAME,
    DB_HOST,
    DB_USERNAME,
    DB_PASSWORD,
    DB_PASSWORD_ADMIN,
    DB_POPULATE,
    APOLLO_DEBUG,
    LOG_LEVEL,
    LOG_DIR,
    LOG_PRETTY,
    LOG_DEFAULT,
    HEALTH_PRETTY_PRINT,
    JWT_EXPIRES_IN,
    JWT_ISSUER,
    SMTP_DEACTIVATED,
    SMTP_HOST,
    SMTP_PORT,
    SMTP_LOG,
    START_DB_SERVER,
    USER_PASSWORD_ENCODED,
};
//# sourceMappingURL=env.js.map